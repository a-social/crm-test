import 'package:flutter/material.dart';

class UnitIcons {
  static final Icon loginMailIcon = Icon(Icons.mail);
  static final Icon loginPasswordIcon = Icon(Icons.lock);
  final IconData loginPasswordObscureIcon = (Icons.visibility);
  final IconData loginPasswordObscureNotShowIon = (Icons.visibility_off);

// Yeni Yatırım Modülü Eylemleri
  static const Icon fastButtonAddInvestor = Icon(Icons.person_add_alt_1);
  static const Icon fastButtonSendInvestmentProposal = Icon(Icons.send);
  static const Icon fastButtonPortfolioAnalysis = Icon(Icons.pie_chart);
  static const Icon fastButtonRiskAnalysis = Icon(Icons.warning_amber);
  static const Icon fastButtonStartInvestment = Icon(Icons.trending_up);
  static const Icon fastButtonContractManagement = Icon(Icons.assignment);
  static const Icon fastButtonInvestmentMeeting = Icon(Icons.video_call);
  static const Icon fastButtonFundTransfer = Icon(Icons.monetization_on);
  static const Icon fastButtonMarketAnalysis = Icon(Icons.bar_chart);

  // Yeni Modüller
  static const Icon riskManagementSystem = Icon(Icons.shield);
  static const Icon marketChartModule = Icon(Icons.show_chart);
  static const Icon customerEducationModule = Icon(Icons.school);
  static const Icon investmentStrategyModule = Icon(Icons.auto_graph);
}
